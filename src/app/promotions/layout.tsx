import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Promosi AIHAA | Tawaran Istimewa Penapis Air",
  description: "Promosi penapis air AIHAA — sekali bayar dari RM399. Trade-in, pakej bundle, program rujukan. Sementara stok masih ada.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
